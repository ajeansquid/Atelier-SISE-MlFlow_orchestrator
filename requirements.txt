# =============================================================================
# Atelier MLflow + Orchestrateurs - Dépendances
# =============================================================================
#
# Avec uv (recommandé) :
#   uv pip install -r requirements.txt
#
# Avec pip :
#   pip install -r requirements.txt
#
# Ou utiliser pyproject.toml :
#   uv sync
#   uv sync --extra prefect      # Ajouter Prefect
#   uv sync --extra orchestrators # Ajouter tous les orchestrateurs
#
# =============================================================================

# Data Science de base
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# MLflow
mlflow>=3.9.0

# Orchestrateurs
prefect>=2.14.0
dagster>=1.5.0
dagster-webserver>=1.5.0

# Airflow (optionnel - lourd, décommenter si nécessaire)
# apache-airflow>=2.7.0

# Formats de données
pyarrow>=14.0.0

# Jupyter (pour les notebooks VS Code)
ipykernel>=6.25.0

# Configuration d'environnement
python-dotenv>=1.0.0
