# =============================================================================
# MLflow + Orchestrators Workshop - Dependencies
# =============================================================================
#
# With uv (recommended):
#   uv pip install -r requirements.txt
#
# With pip:
#   pip install -r requirements.txt
#
# Or use pyproject.toml:
#   uv sync
#   uv sync --extra prefect      # Add Prefect
#   uv sync --extra orchestrators # Add all orchestrators
#
# =============================================================================

# Core Data Science
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# MLflow
mlflow>=2.9.0

# Orchestrators
prefect>=2.14.0
dagster>=1.5.0
dagster-webserver>=1.5.0

# Airflow (optional - heavy, uncomment if needed)
# apache-airflow>=2.7.0

# Data formats
pyarrow>=14.0.0

# Jupyter (for VS Code notebooks)
ipykernel>=6.25.0
