# =============================================================================
# Atelier MLflow + Orchestrateurs - Configuration d'Environnement
# =============================================================================
# Copiez ce fichier et renommez-le en .env (sans le .example)
# Ou en terminal :
#   Mac/Linux : cp .env.example .env
#   Windows   : copy .env.example .env
# =============================================================================

# Configuration des ports (modifier si les ports sont déjà utilisés)
MLFLOW_PORT=5000
PREFECT_PORT=4200
DAGSTER_PORT=3000

# =============================================================================
# ⚠️  UTILISATEURS MAC : Port 5000 déjà utilisé !
# =============================================================================
# macOS Monterey+ utilise le port 5000 pour AirPlay Receiver.
#
# Option 1 (recommandée) : Changez le port MLflow
#   - Modifiez MLFLOW_PORT=5050 ci-dessus
#   - Modifiez MLFLOW_TRACKING_URI=http://localhost:5050 ci-dessous
#
# Option 2 : Désactivez AirPlay Receiver
#   - Préférences Système > Général > AirDrop & Handoff > AirPlay Receiver : OFF
# =============================================================================

# =============================================================================
# URLs des services (utilisées par les scripts Python)
# =============================================================================
# Ces URLs permettent aux scripts locaux de communiquer avec les services Docker
# ⚠️  Si vous changez MLFLOW_PORT ci-dessus, mettez à jour l'URL ci-dessous !

# MLflow Tracking Server
MLFLOW_TRACKING_URI=http://localhost:5000

# Prefect API Server (IMPORTANT: sans cette config, vos flows ne seront pas visibles !)
PREFECT_API_URL=http://localhost:4200/api

# =============================================================================
# Notifications (optionnel - Étape 7)
# =============================================================================
# Décommentez et remplissez pour activer les alertes Discord/Slack

# Discord : Paramètres du serveur > Intégrations > Webhooks > Nouveau webhook
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/VOTRE_ID/VOTRE_TOKEN

# Slack : api.slack.com/apps > Incoming Webhooks > Add New Webhook to Workspace
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/VOTRE/WEBHOOK/URL
